checkpoint: ???

data:
  data_dir: data
  seq_len: null          # null이면 checkpoint hparams.seq_len 사용
  batch_size: 64
  num_workers: 4
  stride: null
  processed_dir_name: processed
  operations: charge,discharge

probe:
  lr: 1e-3
  weight_decay: 0.0

trainer:
  max_epochs: 100
  accelerator: auto
  devices: auto
  patience: 50
  seed: 42

report:
  dir: current_soh_reports
  use_clearml: true
