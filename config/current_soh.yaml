checkpoint: ???
projection_jepa_checkpoint: ???
ts_jepa_checkpoint: ???
mae_checkpoint: ???

data:
  data_dir: data
  seq_len: null          # null이면 checkpoint hparams.seq_len 사용
  batch_size: 64
  num_workers: 4
  stride: null
  processed_dir_name: processed
  operations: charge,discharge

probe:
  lr: 1e-3
  weight_decay: 0.0

experiments:
  pretrained_frozen_attention: false
  random_frozen: true
  random_frozen_attention: false
  random_supervised: true
  pretrained_finetune: true
  pretrained_projection_jepa_frozen: true
  pretrained_ts_jepa_frozen: true
  pretrained_mae_frozen: true

trainer:
  max_epochs: 500
  accelerator: auto
  devices: auto
  patience: 50
  seed: 42

report:
  dir: current_soh_reports
  use_clearml: true
