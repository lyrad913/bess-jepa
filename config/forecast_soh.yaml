checkpoint: ???
projection_jepa_checkpoint: ???
context_mask_jepa_checkpoint: ???
context_mask_projection_jepa_checkpoint: ???
context_mask_projection_jepa_probe_checkpoint: null
context_query_projection_jepa_checkpoint: ???
context_query_projection_jepa_probe_checkpoint: null
ts_jepa_checkpoint: ???
ts_jepa_sigreg_checkpoint: ???
mae_checkpoint: ???

data:
  data_dir: data
  seq_len: 320          # null이면 checkpoint hparams.seq_len 사용
  batch_size: 64
  num_workers: 4
  stride: null
  processed_dir_name: processed
  operations: discharge
  max_horizon: 100       # null이면 가능한 모든 미래 cycle pair 사용
  horizon_stride: 5
  dt_scale: 300.0

probe:
  lr: 1e-3
  weight_decay: 0.0

experiments:
  pretrained_frozen: true
  pretrained_frozen_attention: false
  random_frozen: true
  random_frozen_attention: false
  random_supervised: true
  pretrained_finetune: true
  pretrained_projection_jepa_frozen: true
  pretrained_context_mask_jepa_frozen: true
  pretrained_context_mask_projection_jepa_frozen: true
  pretrained_context_mask_projection_jepa_finetune: true
  pretrained_context_query_projection_jepa_frozen: true
  pretrained_context_query_projection_jepa_finetune: true
  pretrained_ts_jepa_frozen: true
  pretrained_ts_jepa_sigreg_frozen: true
  pretrained_mae_frozen: true

trainer:
  max_epochs: 500
  accelerator: auto
  devices: auto
  patience: 50
  seed: 42

report:
  dir: forecast_soh_reports
  use_clearml: true
